#include "stocklocales.h"

// All locale data is defined inline in the header (stocklocales.h).
// This .cpp is kept so CMake finds a translation unit for the header's
// non-inline symbols; no additional code is needed here.